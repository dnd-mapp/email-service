info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{SERVER_URL}}/email-templates/:templateId/variables"
  params:
    - name: templateId
      value: 4PfN3529W6OScJhGa62H1
      type: path
  body:
    type: json
    data: |
      {
        "name": "username"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
