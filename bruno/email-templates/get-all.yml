info:
  name: get-all
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_URL}}/email-templates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
