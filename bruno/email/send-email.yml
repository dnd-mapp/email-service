info:
  name: send-email
  type: http
  seq: 1

http:
  method: POST
  url: "{{SERVER_URL}}/email"
  body:
    type: json
    data: |
      {
        "to": "oscar.wellner@gmail.com",
        "templateName": "request-change-password",
        "variables": {
          "resetLink": "https://dev.dndmapp.nl.eu.org",
          "username": "NoNamer777"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
